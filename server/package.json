{
  "name": "@evomedia/videostroll",
  "version": "0.0.0-alpha.1",
  "private": true,
  "description": "Agent-driven narrated walkthrough videos of websites - MCP server. Visible cursor, spoken narration, timestamped captions.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "videostroll": "dist/index.js"
  },
  "files": [
    "dist",
    "schema"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "ffmpeg-static": "^5.3.0",
    "playwright": "^1.62.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
